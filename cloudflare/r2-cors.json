{
  "rules": [
    {
      "allowed": {
        "origins": [
          "https://ps-jewellers-mudita-diamond-collect.vercel.app",
          "http://localhost:3000"
        ],
        "methods": ["PUT", "GET", "HEAD"],
        "headers": ["Content-Type"]
      },
      "exposeHeaders": ["ETag"],
      "maxAgeSeconds": 3600
    }
  ]
}
